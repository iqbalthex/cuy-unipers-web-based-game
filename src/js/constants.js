const DISPLAY_PIXEL = { width: 64, height: 64 };
const ASPECT_RATIO  = { width: 16, height: 9 };

const groundConfig = {
  color: "lightblue",
  height: DISPLAY_PIXEL.height * ASPECT_RATIO.height,
  width:  DISPLAY_PIXEL.width  * ASPECT_RATIO.width,
};
